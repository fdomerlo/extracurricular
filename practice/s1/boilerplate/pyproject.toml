[project]
name = "consultorio"
version = "0.1.0"
description = "Sistema de gestión de turnos - Extracurricular Semester 1"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "ruff>=0.9.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
