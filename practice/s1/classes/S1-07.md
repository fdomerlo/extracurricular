---
id: S1-07
título: Git en profundidad — ramas y flujo de trabajo
semestre: S1
stack de referencia: git
competencias: [C09, C10]
nivel: L3
duración estimada: 1 sesión de mentoría (~1.5 h) + práctica individual
prerrequisitos: S1-01 (init/add/commit ya cubiertos ahí)
recorte aplicado: no se repite init/add/commit — eso ya está resuelto en S1-01 y se viene usando desde entonces. Foco en ramas, merge y resolución de conflictos.
---

# S1-07 — Git en profundidad: ramas y flujo de trabajo

## Contexto

Veniste commiteando directo sobre la rama principal desde `S1-01`. Funciona mientras el proyecto es chico y trabajás solo, en línea recta. Deja de funcionar en el momento en que querés probar algo arriesgado sin romper lo que ya anda, o cuando dos cambios tocan el mismo archivo y necesitás integrarlos sin perder ninguno. Esta clase te da esas dos herramientas.

## Concepto mínimo

**Una rama es un puntero, no una copia.** Crear una rama no duplica el proyecto — es liviano, tan liviano que no hay excusa para no usarlas todo el tiempo que haga falta.

```bash
git switch -c feature/cancelacion-tardia
```

**Merge integra el trabajo de vuelta.** Si la rama principal no cambió mientras trabajabas en la tuya, el merge es *fast-forward* — simplemente mueve el puntero, sin crear un commit nuevo. Si sí cambió, git crea un *merge commit* que junta ambas historias.

**Conflicto de merge.** Pasa cuando la misma línea (o líneas cercanas) del mismo archivo cambió en las dos ramas. Git no adivina cuál versión es la correcta — te lo deja marcado:

```
<<<<<<< HEAD
duracion_minutos: int = 30
=======
duracion_minutos: int = 20
>>>>>>> feature/cancelacion-tardia
```

Resolverlo es editar el archivo a mano hasta que quede como debería quedar (no necesariamente ninguna de las dos versiones tal cual — a veces es una combinación), borrar las marcas `<<<<<<<`, `=======`, `>>>>>>>`, y commitear.

**Mensajes de commit, en serio.** Ya venís escribiendo mensajes desde `S1-01`. La convención que vale la pena adoptar ahora: primera línea corta, en modo imperativo ("Agregar validación de horario", no "Agregué" ni "agregando"), y si el *por qué* no es obvio desde el *qué*, un cuerpo debajo explicándolo.

## Demostración

El mentor crea una rama, hace un cambio en una línea de `Practica.duracion_minutos`, vuelve a `main`, hace un cambio *distinto* en la misma línea, y mergea la rama — provocando el conflicto a propósito. Resuelve en vivo, mostrando `git status` durante el conflicto (marca los archivos afectados) y `git log --oneline --graph --all` antes y después para visualizar cómo quedó la historia.

## Laboratorio

Con guía: crear una rama para un cambio chico (agregar un docstring a una función existente), mergearla de vuelta con fast-forward (sin conflicto), y confirmar en `git log --graph` que no se creó un merge commit.

## Ejercicio

Sin guía: crear dos ramas desde el mismo punto, hacer un cambio que toque la misma línea de un mismo archivo en cada una, mergear la primera sin problema, y mergear la segunda — que va a generar un conflicto real. Resolverlo solo.

## Challenge

Investigá `git rebase` como alternativa a merge para limpiar el historial de una rama local antes de integrarla. Regla que tenés que poder justificar, no solo repetir: nunca hacer rebase de una rama que ya compartiste con alguien más (ahí sí, con mentor, discutir por qué).

## Project Task

1. Agregar al `CONVENTIONS.md` de `S1-02` una sección "Flujo de git": de acá en adelante, cambios no triviales van en una rama con prefijo `feature/` o `fix/`, se mergean a `main` cuando están completos y verificados.
2. Aplicar ese flujo retroactivamente al menos una vez: elegir un cambio pendiente real del proyecto y hacerlo en una rama.
3. Commit del `CONVENTIONS.md` actualizado.

## Evidencia — Definition of Done

- [ ] Creaste al menos una rama, la mergeaste con fast-forward.
- [ ] Provocaste un conflicto real (no simulado por el mentor) y lo resolviste vos.
- [ ] `git log --graph` del proyecto muestra al menos una rama integrada.
- [ ] `CONVENTIONS.md` documenta el flujo de ramas adoptado.
- [ ] Podés explicar la diferencia entre fast-forward y merge commit con tu propio ejemplo, no el de la demostración.

## Bibliografía de esta clase

- **Pro Git, capítulo 3 — "Git Branching"** — `git-scm.com/book/es/v2`. Cubre esto en profundidad; no hace falta más para lo que pide esta clase. Los capítulos sobre herramientas avanzadas (`bisect`, `stash`, rebase interactivo) quedan para S3.

## Nota sobre abstracción

Git es hoy el estándar de facto de la industria — a diferencia de otras clases de este semestre, acá no hay una "alternativa de stack" real que valga la pena mencionar. El concepto de ramas aisladas e integración posterior existe en cualquier sistema de control de versiones distribuido, pero en la práctica vas a usar git en casi cualquier trabajo.
